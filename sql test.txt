6. Write a query to display all employees whose salary is greater than 10,000

select * from employees where salary >'10000';

7 Display the names of employees who work in DepartmentID = 20.

select  first_name,department_id from employees where department_id=20;

8. Write a query to show employee names in ascending order.

select * from employees order by first_name asc ;

9 Display only the first 4 characters of each employee’s name.

SELECT first_name, SUBSTRING(first_name, 1, 4) AS short_name
FROM employees;

10  Find the position of the letter 'a' in each employee name.

SELECT first_name,
       LOCATE('a', first_name) AS position
FROM employees;